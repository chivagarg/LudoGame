import Foundation

/// Placeholder for future real-money coin top-ups. Pack pricing lives on `PawnPack` / `UnlockManager`.
enum CoinPurchaseConfig {
    static let coinsPerUnit: Int = 1000
    static let pricePerUnit: Double = 0.99
}
